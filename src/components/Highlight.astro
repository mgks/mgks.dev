---
import { HIGHLIGHT } from '../data/config';
---

{HIGHLIGHT.enabled && (
    <section class="highlight-section">
        <div class="highlight-glow"></div>
        
        <div class="highlight-card">
            <div class="highlight-content">
                <div class="highlight-label">
                    <span class="pulse-dot"></span>
                    <span>{HIGHLIGHT.title}</span>
                </div>
                
                <h2 class="highlight-title">
                    <a href={HIGHLIGHT.link} target="_blank" rel="noopener noreferrer" style="color: inherit; text-decoration: none;">
                        {HIGHLIGHT.project}
                    </a>
                </h2>
                
                <p class="hero-desc">
                    {HIGHLIGHT.description}
                </p>

                <div class="tag-row">
                    {HIGHLIGHT.tags.map(tag => (
                        <span class="tag-pill">{tag}</span>
                    ))}
                </div>

                <a href={HIGHLIGHT.link} target="_blank" rel="noopener noreferrer" class="highlight-link">
                    Check it out &rarr;
                </a>
            </div>

            {HIGHLIGHT.image && (
                <a href={HIGHLIGHT.link} target="_blank" rel="noopener noreferrer" class="highlight-image-wrap" style="display: block;">
                    <img 
                        src={HIGHLIGHT.image} 
                        alt={HIGHLIGHT.project} 
                        class="highlight-image"
                    />
                </a>
            )}
        </div>
    </section>
)}